---
title: "树叶分类竞赛技术总结"
tags: [CV, Deep-Learning, PyTorch, Competition]
---

# 树叶分类竞赛技术总结

> 来源：李沐《动手学深度学习》实战项目

---

## 1. 竞赛背景

### 1.1 任务描述

- **目标**：识别不同种类的树叶
- **数据**：约 18,000 张图片，176 个类别
- **评估**：分类准确率

### 1.2 挑战

- 类别多（176类）
- 样本不均衡
- 部分类别相似度高

---

## 2. 数据增强

### 2.1 训练增强

```python
from torchvision import transforms

train_transform = transforms.Compose([
    transforms.RandomResizedCrop(224),
    transforms.RandomHorizontalFlip(),
    transforms.RandomVerticalFlip(),
    transforms.RandomRotation(30),
    transforms.ColorJitter(brightness=0.2, contrast=0.2),
    transforms.ToTensor(),
    transforms.Normalize([0.485, 0.456, 0.406],
                        [0.229, 0.224, 0.225])
])
```

### 2.2 测试增强

```python
test_transform = transforms.Compose([
    transforms.Resize(256),
    transforms.CenterCrop(224),
    transforms.ToTensor(),
    transforms.Normalize([0.485, 0.456, 0.406],
                        [0.229, 0.224, 0.225])
])
```

---

## 3. 模型选择

### 3.1 基础模型

| 模型 | 精度 | 速度 |
|------|------|------|
| ResNet-50 | 85% | 快 |
| EfficientNet-B4 | 88% | 中 |
| ConvNeXt-Base | 90% | 慢 |

### 3.2 迁移学习

```python
import torchvision.models as models

# 加载预训练模型
model = models.efficientnet_b4(pretrained=True)

# 修改分类头
num_classes = 176
model.classifier[1] = nn.Linear(model.classifier[1].in_features, num_classes)
```

---

## 4. 训练策略

### 4.1 损失函数

```python
# 处理类别不平衡
criterion = nn.CrossEntropyLoss(weight=class_weights)
```

### 4.2 学习率调度

```python
scheduler = optim.lr_scheduler.CosineAnnealingWarmRestarts(
    optimizer, T_0=10, T_mult=2
)
```

### 4.3 标签平滑

```python
criterion = nn.CrossEntropyLoss(label_smoothing=0.1)
```

---

## 5. 集成学习

### 5.1 模型集成

```python
# 多模型预测取平均
def ensemble_predict(models, x):
    predictions = []
    for model in models:
        pred = model(x)
        predictions.append(torch.softmax(pred, dim=1))
    return torch.stack(predictions).mean(dim=0)
```

### 5.2 测试时增强 (TTA)

```python
def tta_predict(model, image, n_augments=5):
    predictions = []
    for _ in range(n_augments):
        aug_image = transform(image)  # 随机增强
        pred = model(aug_image)
        predictions.append(pred)
    return torch.stack(predictions).mean(dim=0)
```

---

## 6. 最终成绩

| 方法 | 单模型 | 集成后 |
|------|--------|--------|
| EfficientNet-B4 | 88.5% | 90.2% |
| ConvNeXt-Base | 90.1% | 91.8% |
| 最终提交 | - | **92.5%** |

---

## 参考资料

- [Kaggle 树叶分类竞赛](https://www.kaggle.com/c/leaf-classification)
- [EfficientNet Paper](https://arxiv.org/abs/1905.11946)

---
title: "多模态：视觉语言模型"
description: "理解图文多模态大模型"
tags: [Multimodal, Vision-Language, CLIP, LLaVA]
---

# 👁️ 多模态：视觉语言模型

> **让AI同时理解图像和文字**

---

## 📝 前言

传统大模型只能处理文本。但现实世界是多模态的——我们通过图像、文字、声音等多种方式获取信息。

**视觉语言模型（VLM）** 让AI能够：
- 看懂图片并描述
- 回答关于图片的问题
- 根据文字生成图片

---

## 🔰 1. 多模态基础

### 1.1 什么是多模态？

| 模态 | 示例 |
|------|------|
| 文本 | 文章、对话、代码 |
| 图像 | 照片、图表、截图 |
| 音频 | 语音、音乐 |
| 视频 | 影片、动画 |

### 1.2 生活类比

想象你是一个翻译：
- **单模态**：只懂中文
- **多模态**：懂中文、英文、日文，还能看图说话

---

## 🔰 2. CLIP 原理

### 2.1 核心思想

**CLIP = Contrastive Language-Image Pre-training**

通过对比学习，让图像和文本在同一个向量空间中。

### 2.2 工作原理

```
训练数据：
- 图像A + "一只猫"
- 图像B + "一只狗"
- 图像C + "一辆车"

训练目标：
- 图像A和"一只猫"的向量应该接近
- 图像A和"一只狗"的向量应该远离
```

### 2.3 架构

```
图像 → 图像编码器 → 图像向量 ─┐
                               ├→ 对比损失
文本 → 文本编码器 → 文本向量 ─┘
```

### 2.4 代码示例

```python
import torch
from PIL import Image
from transformers import CLIPProcessor, CLIPModel

# 加载模型
model = CLIPModel.from_pretrained("openai/clip-vit-base-patch32")
processor = CLIPProcessor.from_pretrained("openai/clip-vit-base-patch32")

# 加载图像
image = Image.open("cat.jpg")

# 文本候选
texts = ["一只猫", "一只狗", "一辆车"]

# 处理输入
inputs = processor(
    text=texts,
    images=image,
    return_tensors="pt",
    padding=True
)

# 推理
outputs = model(**inputs)
logits_per_image = outputs.logits_per_image

# 计算概率
probs = logits_per_image.softmax(dim=1)
print("预测结果：")
for text, prob in zip(texts, probs[0]):
    print(f"{text}: {prob:.2%}")
```

---

## 🔰 3. LLaVA 原理

### 3.1 什么是LLaVA？

**LLaVA = Large Language and Vision Assistant**

结合视觉编码器和大语言模型，实现图像理解和对话。

### 3.2 架构

```
图像 → 视觉编码器(CLIP) → 视觉特征
                              ↓
                        投影层 → 视觉token
                              ↓
文本 → 文本tokenizer → 文本token ─┘→ LLM → 回答
```

### 3.3 核心创新

1. **视觉-语言对齐**：通过投影层连接视觉和语言
2. **指令微调**：在对话数据上微调
3. **端到端训练**：整个系统可以联合优化

---

## 🔰 4. 多模态模型对比

### 4.1 主流模型

| 模型 | 公司 | 特点 |
|------|------|------|
| GPT-4V | OpenAI | 最强，闭源 |
| LLaVA | 开源 | 开源、可本地部署 |
| Qwen-VL | 阿里 | 中文优化 |
| InternVL | 上海AI Lab | 开源、多语言 |

### 4.2 选择建议

| 场景 | 推荐模型 |
|------|----------|
| 追求最佳效果 | GPT-4V |
| 需要本地部署 | LLaVA |
| 中文场景 | Qwen-VL |

---

## 🔰 5. 实战：图像理解

### 5.1 使用 LLaVA

```python
from transformers import LlavaForConditionalGeneration, AutoProcessor

# 加载模型
model = LlavaForConditionalGeneration.from_pretrained(
    "llava-hf/llava-1.5-7b-hf"
)
processor = AutoProcessor.from_pretrained("llava-hf/llava-1.5-7b-hf")

# 加载图像
image = Image.open("photo.jpg")

# 构建对话
conversation = [
    {
        "role": "user",
        "content": [
            {"type": "image"},
            {"type": "text", "text": "请描述这张图片"}
        ]
    }
]

# 处理输入
prompt = processor.apply_chat_template(conversation, add_generation_prompt=True)
inputs = processor(images=image, text=prompt, return_tensors="pt")

# 生成回答
output = model.generate(**inputs, max_new_tokens=200)
response = processor.decode(output[0], skip_special_tokens=True)
print(response)
```

### 5.2 使用 Qwen-VL

```python
from transformers import AutoModelForCausalLM, AutoTokenizer

# 加载模型
model = AutoModelForCausalLM.from_pretrained(
    "Qwen/Qwen-VL-Chat",
    device_map="auto"
)
tokenizer = AutoTokenizer.from_pretrained("Qwen/Qwen-VL-Chat")

# 图像URL
image_url = "https://example.com/photo.jpg"

# 对话
query = tokenizer.from_list_format([
    {'image': image_url},
    {'text': '描述这张图片'}
])

response, history = model.chat(tokenizer, query=query, history=None)
print(response)
```

---

## 🔰 6. 多模态应用场景

### 6.1 常见应用

| 应用 | 说明 |
|------|------|
| 图像描述 | 为图片生成文字描述 |
| 视觉问答 | 回答关于图片的问题 |
| OCR | 图片中的文字识别 |
| 文档理解 | 理解PDF、截图内容 |
| 商品搜索 | 以图搜图 |

### 6.2 代码示例：文档理解

```python
def understand_document(image_path, question):
    """理解文档图片"""
    image = Image.open(image_path)
    
    prompt = f"""
    这是一份文档的截图。
    
    问题：{question}
    
    请根据文档内容回答：
    """
    
    # 使用GPT-4V或LLaVA
    response = call_multimodal_model(image, prompt)
    return response

# 使用
answer = understand_document(
    "invoice.jpg",
    "这张发票的总金额是多少？"
)
```

---

## 📚 本节小结

| 概念 | 说明 |
|------|------|
| 多模态 | 同时处理多种信息类型 |
| CLIP | 图文对比学习 |
| LLaVA | 视觉语言助手 |
| VLM | 视觉语言模型 |

---

## 🎯 下一步

- **22b - 多模态：语音与视频** - 语音识别、视频理解
- **23 - 智能体通信协议** - MCP、A2A等

---

> 💡 **实践建议**：用LLaVA或Qwen-VL尝试图像描述和视觉问答任务。
